import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("Hive")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
    }
}

#Preview {
    ContentView()
}
